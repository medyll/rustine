---
story_id: S1-01
title: Add rustine.db to .gitignore
status: in_progress
assignee: Developer
---

Why

Prevent accidental check-in of the local SQLite file used during development.

Acceptance

- `.gitignore` contains an entry `rustine.db` at project root.
- Project builds and tests run unchanged.

Tasks

- [ ] Add `rustine.db` to `.gitignore`
- [ ] Run `cargo check` to verify nothing else required
